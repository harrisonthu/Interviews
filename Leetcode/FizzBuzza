Acutally this question is from Hackerrank

import java.io.*;
import java.util.*;

public class Solution {

    public static void main(String[] args) {
        /* Enter your code here. Read input from STDIN. Print output to STDOUT. Your class should be named Solution. */

        //Scanner sc = new Scanner(System.in);
        //int myint = sc.nextInt();
        
        // Since we need to provide the 1-100, since it is not coming from system.in , use for loop
        for (int i =1; i<=100; i++){        
            // check if myint is divisible by 3 or 5
            if (i%15 ==0){
                System.out.println("FizzBuzz");
            }
            else if (i %5 ==0){
                System.out.println("Buzz");
            }
            else if (i %3 ==0){
                System.out.println("Fizz");
            }
            // not divisible by 3 or 5 or both, so return just number
            else {
                System.out.println(i);
            }
        }


    }
}

